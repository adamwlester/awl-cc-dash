# Never commit the auth cookie (extract-web.py). Pulled conversations now go to the
# gitignored <repo>/transcripts/, not a local out/ — so only the cookie + bytecode are here.
session_key.txt
__pycache__/
*.pyc
