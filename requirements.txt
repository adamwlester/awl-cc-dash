# AWL Dashboard — Python dependencies (sidecar + bridge + tests)
#
# The sidecar (sidecar/main.py) is a FastAPI service that drives the Claude Agent SDK.
# Versions are pinned to the known-good set verified in the frozen MVP (archive/mvp).
#
# Setup:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# The Claude Agent SDK shells out to the `claude` CLI — it must be on PATH.

# Sidecar (FastAPI service)
claude-agent-sdk==0.2.106
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
sse-starlette==3.4.5
python-dotenv==1.2.2

# Testing
pytest==9.1.1
