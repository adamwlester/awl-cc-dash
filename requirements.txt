# AWL Dashboard — Python dependencies (sidecar + bridge + tests)
#
# The sidecar (sidecar/main.py) is a FastAPI service that drives the Claude Agent SDK.
# Versions are pinned to the known-good set verified in the frozen MVP (archive/mvp).
#
# Setup:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# The Claude Agent SDK shells out to the `claude` CLI — it must be on PATH.

# Sidecar (FastAPI service)
claude-agent-sdk==0.2.106
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
sse-starlette==3.4.5
python-dotenv==1.2.2

# Testing
pytest==9.1.1

# Live spike deps (opt-in tier; see tests/README.md). Pinned to the known-good set
# the 2026-07-02 spikes were verified against.
#   playwright drives the UI-slice spike (tests/ui/test_ui_slice_live.py). The pip
#   package alone is not enough — also fetch the browser binary once:
#       .venv\Scripts\playwright install chromium
playwright==1.61.0
#   psutil gives the polling-scale spike (tests/test_polling_scale_ceiling_live.py)
#   real CPU sampling; without it that spike falls back to WSL /proc/stat.
psutil==5.9.8
